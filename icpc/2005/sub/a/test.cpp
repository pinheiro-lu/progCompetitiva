#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;

#define f first
#define s second

int n, m;
float v;

signed main() {
	ios_base::sync_with_stdio(0); cin.tie(0);
		for (int i = 1; i <= 100; ++i) {
			cout << (i + 99) % 100 / 4 << '\n';
		}


	return 0;
}
