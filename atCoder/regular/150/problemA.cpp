#include <bits/stdc++.h>
using namespace std;

#define debug(args...) printf(args)
#define endl '\n'

typedef long long ll;

#define MAX 30

bool valid(string s, int n, int k) {
	for (int i = 0; i < n; i++) {
		
	}
}

void solve(void) {
	int n, k;
	string s;
	
	cin >> n >> k;
	cin >> s;
	
	if (s,n,k) cout << "Yes" << endl;
	else cout << "No" << endl;
	
}

int main () {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	int t;
	
	cin >> t;
	
	while (t--)
		solve();
		
	return 0;
}
